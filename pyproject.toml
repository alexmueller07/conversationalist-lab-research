[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "convlab"
version = "0.1.0"
description = "Multimodal measurement of dyadic conversation quality from three-camera recordings"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Alexander Mueller", email = "admueller3@wisc.edu" }]
keywords = ["conversation analysis", "turn-taking", "multimodal", "psychology", "dyadic"]

dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "pandas>=2.0",
    "pyarrow>=12.0",
    "av>=11.0",
    "opencv-python>=4.8",
    "onnxruntime>=1.16",
    "mediapipe>=0.10",
    "faster-whisper>=1.0",
    "praat-parselmouth>=0.4.3",
    "soundfile>=0.12",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
semantic = ["sentence-transformers>=2.2", "torch>=2.0"]
dev = ["pytest>=7.4", "pytest-cov>=4.1"]

[project.scripts]
convlab = "convlab.cli:main"

[project.gui-scripts]
convlab-gui = "convlab.gui:main"
# A gui-script rather than a console script: on Windows this produces a
# launcher that opens no console window, so double-clicking the app does not
# leave a black terminal sitting behind it.

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
convlab = ["resources/*.json", "resources/*.txt", "report/assets/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
markers = [
    "slow: tests that download models or decode media",
    "needs_models: tests requiring fetched model assets",
]
